apiVersion: v1
kind: Secret
metadata:
  name: test-service-secrets
  labels:
    app: test-service
type: Opaque
data:
  # No secrets needed for test service
  # Add secrets here if needed in the future
