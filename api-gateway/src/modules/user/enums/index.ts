export enum Role {
    ADMIN,
    EDITOR,
    USER
}

export enum UserStatus {
    UNVERIFIED,
    VERIFIED,
    BANNED,
    INACTIVE,
}

export enum OAuthProvider {
    GITHUB,
    GOOGLE,
    FACEBOOK
}
